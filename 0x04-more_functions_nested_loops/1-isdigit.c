#include "main.h"

/**
* _isdigit -> checks if the integer is a digit 0-9
* @n: an integer argument
* Return: returns 1 and 0 depending on condition
*/
int _isdigit(int n)
{
	return (n >= '0' && n <= '9');
}
